# Chương 18 — Code mẫu: Blazor

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Trang: `/dem`, `/san-pham` (cần trình duyệt để có tương tác).
