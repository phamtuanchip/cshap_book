# Chương 4 — Code mẫu: Minimal API (CRUD Todo)

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. OpenAPI: `/openapi/v1.json` (chỉ ở môi trường Development).
