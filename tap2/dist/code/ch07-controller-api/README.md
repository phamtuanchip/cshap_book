# Chương 7 — Code mẫu: Web API với Controller

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. File `customers.http` chứa sẵn các request mẫu.
