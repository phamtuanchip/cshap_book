# Chương 16 — Code mẫu: Razor Pages

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Tạo file `razor.db` khi chạy lần đầu.
