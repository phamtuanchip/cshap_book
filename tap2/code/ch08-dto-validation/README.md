# Chương 8 — Code mẫu: DTO và Validation

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Gửi JSON sai tới `POST /api/san-pham` và `/minimal/san-pham` để xem lỗi validation.
