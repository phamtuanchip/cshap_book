# Chương 17 — Code mẫu: Form và validation

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Thử: `/DangKy` và `POST /api/tai-len` (cần token CSRF từ `GET /api/csrf`).
