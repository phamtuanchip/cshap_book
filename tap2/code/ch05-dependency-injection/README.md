# Chương 5 — Code mẫu: Dependency Injection

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Thử: `/thoi-gian` (gọi 2 lần), `/giam-gia?tien=2000000`, `/gui?kenh=sms&noiDung=hi`.
