# Chương 13 — Code mẫu: Quan hệ và hiệu năng EF Core

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. (Console.) Xem số câu SQL của mỗi cách tải dữ liệu.
