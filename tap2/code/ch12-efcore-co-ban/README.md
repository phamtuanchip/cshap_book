# Chương 12 — Code mẫu: EF Core cơ bản

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. (Console.) Tạo migration mới: `dotnet ef migrations add <Ten>`.
