# Chương 3 — Code mẫu: Middleware

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Thử: `curl -i /cham`, `/bi-chan/x`, `/loi`, `/?debug=1`.
