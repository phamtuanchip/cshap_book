# Chương 1 — Code mẫu: kiến trúc phân lớp

Bộ khung 4 tầng cực nhỏ (`Domain → Application → Infrastructure → Web`) và 6 architecture test.

```
cd KienTruc.Tests
dotnet test
```

Chạy API: `dotnet run --project KienTruc.Web` rồi gọi `/api/san-pham`.
