# Chương 8 — Code mẫu: Agent: vòng lặp suy luận và hành động

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Không cần khoá API (mô hình giả có logic nhiều bước).
