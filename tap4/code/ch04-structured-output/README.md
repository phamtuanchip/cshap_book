# Chương 4 — Code mẫu: Đầu ra có cấu trúc

Yêu cầu: .NET SDK 10. Chạy trong thư mục này:

```
dotnet run
```

Mở địa chỉ hiển thị trong terminal (thường `http://localhost:5xxx`) bằng trình duyệt hoặc `curl`. Không cần khoá API (nhà cung cấp giả có kịch bản).
